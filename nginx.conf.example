# Nginx Configuration for Megas Chat
# Copy this file to /etc/nginx/sites-available/iachat.painelcontrole.xyz
# Then create symlink: ln -s /etc/nginx/sites-available/iachat.painelcontrole.xyz /etc/nginx/sites-enabled/

server {
    listen 80;
    listen [::]:80;
    server_name iachat.painelcontrole.xyz;
    
    root /www/wwwroot/iachat.painelcontrole.xyz;
    index index.php index.html;

    # Logs
    access_log /var/log/nginx/iachat_access.log;
    error_log /var/log/nginx/iachat_error.log;

    # Aumentar tamanho máximo de upload para imagens
    client_max_body_size 10M;

    # Rewrite all requests to index.php (front controller pattern)
    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    # Process PHP files
    location ~ \.php$ {
        include snippets/fastcgi-php.conf;
        fastcgi_pass unix:/run/php/php8.2-fpm.sock;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        include fastcgi_params;
        
        # Increase timeout for long-running requests (Gemini API calls)
        fastcgi_read_timeout 300;
    }

    # Deny access to hidden files (security)
    location ~ /\. {
        deny all;
        access_log off;
        log_not_found off;
    }

    # Deny access to sensitive files
    location ~ (config\.php|composer\.json|composer\.lock|\.git|\.gitignore|\.env) {
        deny all;
        access_log off;
        log_not_found off;
    }

    # Cache static files
    location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg|woff|woff2|ttf|eot)$ {
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
}

# HTTPS Configuration (uncomment after installing SSL certificate)
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name iachat.painelcontrole.xyz;
#     
#     ssl_certificate /etc/letsencrypt/live/iachat.painelcontrole.xyz/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/iachat.painelcontrole.xyz/privkey.pem;
#     
#     # SSL Configuration
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#     
#     root /www/wwwroot/iachat.painelcontrole.xyz;
#     index index.php index.html;
# 
#     access_log /var/log/nginx/iachat_access.log;
#     error_log /var/log/nginx/iachat_error.log;
# 
#     client_max_body_size 10M;
# 
#     location / {
#         try_files $uri $uri/ /index.php?$query_string;
#     }
# 
#     location ~ \.php$ {
#         include snippets/fastcgi-php.conf;
#         fastcgi_pass unix:/run/php/php8.2-fpm.sock;
#         fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
#         include fastcgi_params;
#         fastcgi_read_timeout 300;
#     }
# 
#     location ~ /\. {
#         deny all;
#         access_log off;
#         log_not_found off;
#     }
# 
#     location ~ (config\.php|composer\.json|composer\.lock|\.git|\.gitignore|\.env) {
#         deny all;
#         access_log off;
#         log_not_found off;
#     }
# 
#     location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg|woff|woff2|ttf|eot)$ {
#         expires 30d;
#         add_header Cache-Control "public, immutable";
#     }
# }
# 
# # Redirect HTTP to HTTPS
# server {
#     listen 80;
#     listen [::]:80;
#     server_name iachat.painelcontrole.xyz;
#     return 301 https://$server_name$request_uri;
# }
